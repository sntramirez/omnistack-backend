server.port=8086
logging.level.com.omnistack.backend=INFO
app.integrations.mock-enabled=false
app.integrations.ssl-verification-disabled=true

# Host/IP y puerto donde el POS alcanza este servidor en QA (no localhost) — usado para
# armar la URL publica GET /v1/comprobantes/{id} del comprobante generado en VERIFY.
app.comprobantes.public-base-url=${APP_COMPROBANTES_PUBLIC_BASE_URL:http://10.102.1.16:8086}

# Oracle TUKUNAFUNC — BD prod (IN_OMNI_*: config y logs de proveedores) en PRODUAT_CLOUD_M2C_UIO (PRS6)
app.datasource.prod.url=${APP_DATASOURCE_PROD_URL:jdbc:oracle:thin:@10.100.3.20:1521:PRS6}
app.datasource.prod.username=${APP_DATASOURCE_PROD_USERNAME:TUKUNAFUNC}
app.datasource.prod.password=${APP_DATASOURCE_PROD_PASSWORD:tvkvn4tst}
app.datasource.prod.schema=${APP_DATASOURCE_PROD_SCHEMA:TUKUNAFUNC}

# Oracle RMS — items maestros + catálogo AD_* via gpf_omnistack. (gpf_lectura@MOMQA)
# gpf_lectura tiene grants SELECT sobre gpf_omnistack.AD_* en la BD rms
app.datasource.rms.url=${APP_DATASOURCE_RMS_URL:jdbc:oracle:thin:@(DESCRIPTION=(FAILOVER=ON)(LOAD_BALANCE=ON)(ADDRESS=(PROTOCOL=TCP)(HOST=vmcluts1scan.gfybeca.int)(PORT=1521))(CONNECT_DATA=(SERVICE_NAME=momqa_pdb1.paas.oracle.com)))}
app.datasource.rms.username=${APP_DATASOURCE_RMS_USERNAME:gpf_lectura}
app.datasource.rms.password=${APP_DATASOURCE_RMS_PASSWORD:gpf_lectura1159}

# --- Control de cupos CASH_OUT ---
app.cashout-quota.reservation-timeout-minutes=${APP_CASHOUT_QUOTA_RESERVATION_TIMEOUT_MINUTES:30}
app.cashout-quota.expiration-scheduler-rate-ms=${APP_CASHOUT_QUOTA_EXPIRATION_SCHEDULER_RATE_MS:60000}

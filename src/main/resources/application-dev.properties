server.port=8086
logging.level.com.omnistack.backend=DEBUG
app.integrations.mock-enabled=false
app.integrations.ssl-verification-disabled=true

# Oracle TUKUNAFUNC — BD prod (IN_OMNI_*: config y logs de proveedores)
app.datasource.prod.url=${APP_DATASOURCE_PROD_URL:jdbc:oracle:thin:@//localhost:1521/XEPDB1}
app.datasource.prod.username=${APP_DATASOURCE_PROD_USERNAME:TUKUNAFUNC}
app.datasource.prod.password=${APP_DATASOURCE_PROD_PASSWORD:tvkvn4tst}
app.datasource.prod.schema=${APP_DATASOURCE_PROD_SCHEMA:TUKUNAFUNC}

# Oracle RMS — items maestros + catálogo AD_* via gpf_omnistack. (gpf_lectura en QA; rms en dev local)
# Prerequisito: GRANT SELECT ON gpf_omnistack.AD_* TO rms (script 05_GRANTS_SINONIMOS.sql cubre esto en local)
app.datasource.rms.url=${APP_DATASOURCE_RMS_URL:jdbc:oracle:thin:@//localhost:1521/XEPDB1}
app.datasource.rms.username=${APP_DATASOURCE_RMS_USERNAME:rms}
app.datasource.rms.password=${APP_DATASOURCE_RMS_PASSWORD:rms123}

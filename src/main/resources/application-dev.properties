server.port=8086
logging.level.com.omnistack.backend=DEBUG
app.integrations.mock-enabled=false
app.integrations.ssl-verification-disabled=true

# Oracle TUKUNAFUNC — catálogo compartido (AD_*) en Docker local (XEPDB1)
app.datasource.prod.url=${APP_DATASOURCE_PROD_URL:jdbc:oracle:thin:@//localhost:1521/XEPDB1}
app.datasource.prod.username=${APP_DATASOURCE_PROD_USERNAME:GPF_OMNISTACK}
app.datasource.prod.password=${APP_DATASOURCE_PROD_PASSWORD:F4P_xmtyT_C1MpT}

# Oracle TUKUNAFUNC — transaccional (IN_OMNI_*) en Docker local (XEPDB1)
app.datasource.mom.url=${APP_DATASOURCE_MOM_URL:jdbc:oracle:thin:@//localhost:1521/XEPDB1}
app.datasource.mom.username=${APP_DATASOURCE_MOM_USERNAME:TUKUNAFUNC}
app.datasource.mom.password=${APP_DATASOURCE_MOM_PASSWORD:tvkvn4tst}
app.datasource.mom.schema=${APP_DATASOURCE_MOM_SCHEMA:TUKUNAFUNC}

# Oracle RMS — items maestros (ITEM_MASTER, CLASS, SUBCLASS, SUPS, UDA_*)
# Dev default: schema rms del Docker local. Para usar QA: override APP_DATASOURCE_RMS_* con gpf_lectura@momqa
app.datasource.rms.url=${APP_DATASOURCE_RMS_URL:jdbc:oracle:thin:@//localhost:1521/XEPDB1}
app.datasource.rms.username=${APP_DATASOURCE_RMS_USERNAME:rms}
app.datasource.rms.password=${APP_DATASOURCE_RMS_PASSWORD:rms123}

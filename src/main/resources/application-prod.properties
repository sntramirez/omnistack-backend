server.port=8082
logging.level.com.omnistack.backend=WARN
app.integrations.mock-enabled=false

# Catalogo business-lines — TUKUNAFUNC HPROD (env vars del pod)
app.business-lines.oracle.datasource1.url=${APP_BUSINESS_LINES_ORACLE_DATASOURCE1_URL}
app.business-lines.oracle.datasource1.username=${APP_BUSINESS_LINES_ORACLE_DATASOURCE1_USERNAME}
app.business-lines.oracle.datasource1.password=${APP_BUSINESS_LINES_ORACLE_DATASOURCE1_PASSWORD}
app.business-lines.oracle.datasource1.driver-class-name=oracle.jdbc.OracleDriver

# Transaccional IN_OMNI_* — TUKUNAFUNC HPROD (env vars del pod)
app.datasource.hprod.url=${APP_DATASOURCE_HPROD_URL}
app.datasource.hprod.username=${APP_DATASOURCE_HPROD_USERNAME}
app.datasource.hprod.password=${APP_DATASOURCE_HPROD_PASSWORD}
app.datasource.hprod.driver-class-name=oracle.jdbc.OracleDriver
